name: material_wavy_progress_indicator
description: "Material 3 Wavy Progress Indicator — vendored from github.com/ksokolovskyi/material_wavy_progress_indicator"
version: 1.0.0
publish_to: "none"

resolution: workspace

environment:
  sdk: ^3.10.3
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  material_shapes:
    path: ../material_shapes
