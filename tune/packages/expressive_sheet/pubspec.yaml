name: expressive_sheet
description: "A Material 3 Expressive take on the modal bottom sheet: spring physics with drag-driven dismissal."
version: 1.0.0
publish_to: "none"

resolution: workspace

environment:
  sdk: ^3.10.3
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  motor: ^1.1.0
