/// Centralized asset paths — add entries here as real icons/illustrations
/// are dropped into assets/, instead of stringly-typed paths scattered
/// across widgets.
class AssetValues {
  AssetValues._();

  static const vectors = 'assets/vectors';
  static const images = 'assets/images';

  static const appIcon = '$images/app-icon.png';
}
